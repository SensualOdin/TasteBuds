# Server Configuration
PORT=3000
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# Google Maps API (Required for restaurant search)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Yelp API (Optional - expensive, Google Places is primary source)
# Leave empty to skip Yelp integration
YELP_API_KEY=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload
MAX_FILE_SIZE=5242880

# CORS Configuration
FRONTEND_URL=http://localhost:8081

# Socket.IO Configuration
SOCKET_IO_PORT=3000

# ============================================================================
# MCP SERVER CONFIGURATION (Claude Code)
# ============================================================================
# Supabase MCP is configured in .claude/mcp.json with FULL READ/WRITE access
# After adding MCP config, reload VSCode window to authenticate via OAuth
# This provides:
#   - Natural language database queries (SELECT, INSERT, UPDATE, DELETE)
#   - Schema inspection and migrations
#   - Data manipulation and management
# ⚠️  Write access enabled - Use with caution on production data! 